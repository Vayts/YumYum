export interface IErrorMsg {
	show: boolean,
	margin?: string,
  align?: 'center' | 'right' | 'left',
}

export interface IErrorWrapperStyle {
	margin?: string,
  align?: 'center' | 'right' | 'left',
}
